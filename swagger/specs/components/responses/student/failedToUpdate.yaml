description: Failed to update student with given id
content:
  application/json:
    examples:
      studentNotFound:
        $ref: '../../examples/student/notFound.yaml'
      studentInvalidDataProvided:
        $ref: '../../examples/student/invalidDataProvided.yaml'
