get:
  summary: Get student by id
  parameters:
    - $ref: '../components/parameters/studentID.yaml'
  responses:
    '200':
      description: Successfully retrieved student with given id
      content:
        application/json:
          schema:
            $ref: '../components/schemas/student/student.yaml'
    '400':
      description: Failed to retrieve student with given id 
      content:
        application/json:
          examples:
            studentNotFound:
              $ref: '../components/examples/student/studentNotFound.yaml'
delete:
  summary: Delete student by id
  parameters:
    - $ref: '../components/parameters/studentID.yaml'
  responses:
    '204':
      description: Successfully deleted student with given id
    '400':
      description: Failed to delete student with given id 
      content:
        application/json:
          examples:
            studentNotFound:
              $ref: '../components/examples/student/studentNotFound.yaml'

put:
  summary: Update student by id
  parameters:
    - $ref: '../components/parameters/studentID.yaml'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/schemas/student/student.yaml'
  responses:
    '200':
      description: Successfully updated student with given id
      content:
        application/json:
          schema:
            $ref: '../components/schemas/student/student.yaml'
    '400':
      $ref: '../components/responses/student/failedToUpdate.yaml'
patch:
  summary: Partially update student by id
  parameters:
    - $ref: '../components/parameters/studentID.yaml'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/schemas/student/studentPatch.yaml'
  responses:
    '200':
      description: Successfully updated student with given id
      content:
        application/json:
          schema:
            $ref: '../components/schemas/student/student.yaml'
    '400':
      $ref: '../components/responses/student/failedToUpdate.yaml'
